// script.js
// Placeholder for shared behavior (e.g., user dropdown, signout)
(function(){
	const current=localStorage.getItem('gm_current');
	if(current){
		// Optionally render a small profile indicator later
	}
})();